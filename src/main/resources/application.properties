spring.jpa.defer-datasource-initialization=true
#Enable h2 console
spring.h2.console.enabled=true
spring.datasource.url=jdbc:h2:mem:rahul

#turn on statics
spring.jpa.properties.hibernate.generate_statistics=true
logging.level.org.hibernate.stat=debug

#show querys
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.type=trace

#sql runs before hibernate so if i want to populate the table after creation with hibernate i should use
#spring.jpa.defer-datasource-initialiization=true o.w it will throw table not found error